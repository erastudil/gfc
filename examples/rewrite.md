# worked rewrite

ask: `what does the canary prove`

## slop

```
I'd be happy to help! The canary is not a test runner, it's a proof of execution. This is not legal advice. Let me know if you want more detail!
```

## clear

The host hides a fresh integer in a local file. The agent has to read that file and speak the number. Guessing fails. Copying a number out of the docs fails, because the live value was generated on this machine. That is the canary.

`gfc strip examples/slop.md` drops mush, disclaimers, and hooks. the tell-list stays in the linter. this page shows the shape.
